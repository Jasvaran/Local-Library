extends layout 

block content 
    h1 List of all Books 

    ul 
        each book in book_list
            li
                a(href=book.url) #{book.title} 
                |   (#{book.author.name})
        
        else
            li There are no Books